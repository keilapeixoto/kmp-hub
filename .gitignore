.DS_Store

# Node / Next.js
node_modules/
.next/
next-env.d.ts
*.tsbuildinfo

# Segredos — nunca versionar
.env
.env.local
.env.*.local

# Supabase CLI local
supabase/.branches
supabase/.temp

# Dados reais de clientes (mapeamento de importação) — nunca versionar
import/

# Playwright — sessões de login (storageState) contêm tokens reais, nunca versionar
playwright/.auth/
playwright-report/
test-results/
blob-report/
