/**
 * Página inicial do portal do cliente (Fase 2 do plano). Reservada aqui só
 * como estrutura de rota; conteúdo real chega junto com o resto do portal.
 */
export default function PortalHomePage() {
  return (
    <main className="flex min-h-screen items-center justify-center px-4">
      <p className="text-sm text-kmp-graphite/70">Em construção — Fase 2.</p>
    </main>
  );
}
